# Test Environment Configuration Template
# Copy this file to .env.test and update values as needed

# Test Database Configuration (separate from production)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_test_password
DB_NAME=gent_disagreement_test

# Test API Keys (fake keys for mocked tests - these don't need to be real)
OPENAI_API_KEY=test-key-openai
DEEPGRAM_API_KEY=test-key-deepgram

# Test File Paths
AUDIO_TRANSCRIBER_AUDIO_DIR="./tests/fixtures/audio"
AUDIO_TRANSCRIBER_OUTPUT_DIR="./tests/fixtures/transcripts"
TRANSCRIPT_FORMATTER_OUTPUT_DIR="./tests/fixtures/processed"